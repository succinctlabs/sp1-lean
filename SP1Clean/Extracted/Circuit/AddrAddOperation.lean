import SP1Clean.Math.Word
import SP1Clean.Model.Channels
import SP1Clean.Model.ByteTable
import SP1Clean.Extracted.AddrAddOperation
import Clean.Circuit.Basic
import Clean.Circuit.Subcircuit
import Clean.Circuit.Channel
import Clean.Gadgets.Equality
import Clean.Utils.Tactics.ProvableStructDeriving

/-! # AUTO-GENERATED circuit form — do not edit by hand.

SP1's `AddrAddOperation::eval` as a Clean `Circuit`: the `Inputs` struct (the `eval` params
verbatim — the column struct nested as `cols`), the `main` do-block, and the
`ElaboratedCircuit` instance + `@[circuit_norm]` rfl-lemmas. Generated by
`update_extracted.py` from the `sp1-constraint-compiler` (`--operation AddrAddOperation --format
lean-circuit`). This *is* the faithful artifact the gadget's soundness/completeness run
against — no separate `asserts`/`interactions` bridge. Regenerate with
`SP1_DIR=… python3 update_extracted.py`. -/

set_option linter.all false  -- auto-generated: skip linters

namespace SP1Clean.AddrAddOperation

open Circuit
open SP1Clean.Channels (byteChannel)
open SP1Clean.Extracted

variable {p : ℕ} [Fact p.Prime] [Fact (2 ^ 17 < p)]

structure Inputs (F : Type) where
  a : (Word F)
  b : (Word F)
  cols : (AddrAddOperation F)
  is_real : F
deriving ProvableStruct

def main (input : Var Inputs (ZMod p)) : Circuit (ZMod p) Unit := do
  let a := input.a
  let b := input.b
  let cols := input.cols
  let is_real := input.is_real
  let E0 := is_real - 1
  let E1 := is_real * E0
  let E2 := a[0] + b[0]
  let E3 := E2 - cols.value[0]
  let E4 := E3 + 0
  let E5 := E4 * ((65536 : ZMod p)⁻¹)
  let E6 := E5 - 1
  let E7 := E5 * E6
  let E8 := is_real * E7
  let E9 := a[1] + b[1]
  let E10 := E9 - cols.value[1]
  let E11 := E10 + E5
  let E12 := E11 * ((65536 : ZMod p)⁻¹)
  let E13 := E12 - 1
  let E14 := E12 * E13
  let E15 := is_real * E14
  let E16 := a[2] + b[2]
  let E17 := E16 - cols.value[2]
  let E18 := E17 + E12
  let E19 := E18 * ((65536 : ZMod p)⁻¹)
  let E20 := E19 - 1
  let E21 := E19 * E20
  let E22 := is_real * E21
  let E23 := a[3] + b[3]
  let E24 := E23 - 0
  let E25 := E24 + E19
  let E26 := E25 * ((65536 : ZMod p)⁻¹)
  let E27 := E26 - 1
  let E28 := E26 * E27
  let E29 := is_real * E28
  byteChannel.pullIf is_real (⟨6, cols.value[0], Expression.const ((16 : ℕ) : ZMod p), 0⟩ : ByteRow (Expression (ZMod p)))
  byteChannel.pullIf is_real (⟨6, cols.value[1], Expression.const ((16 : ℕ) : ZMod p), 0⟩ : ByteRow (Expression (ZMod p)))
  byteChannel.pullIf is_real (⟨6, cols.value[2], Expression.const ((16 : ℕ) : ZMod p), 0⟩ : ByteRow (Expression (ZMod p)))
  E1 === 0
  E8 === 0
  E15 === 0
  E22 === 0
  E29 === 0

instance elaborated : ElaboratedCircuit (ZMod p) Inputs unit main where
  localLength _ := 0
  output _ _ := ()
  channelsWithGuarantees := [byteChannel.toRaw]
  channelsWithRequirements := [byteChannel.toRaw]

set_option linter.unusedSectionVars false in
@[circuit_norm] lemma channelsWithGuarantees_eq :
    ((elaborated (p := p)).channelsWithGuarantees : List (RawChannel (ZMod p)))
      = [byteChannel.toRaw] := rfl
set_option linter.unusedSectionVars false in
@[circuit_norm] lemma channelsWithRequirements_eq :
    ((elaborated (p := p)).channelsWithRequirements : List (RawChannel (ZMod p)))
      = [byteChannel.toRaw] := rfl
set_option linter.unusedSectionVars false in
@[circuit_norm] lemma localLength_eq (x : Var Inputs (ZMod p)) :
    (elaborated (p := p)).localLength x = 0 := rfl

end SP1Clean.AddrAddOperation
