import SP1Clean.Math.Word
import SP1Clean.Extracted.ExtractionDSL
import Clean.Utils.Tactics.ProvableStructDeriving

/-! # AUTO-GENERATED system-table Rust AIR oracle — do not edit by hand.

Generated by `update_extracted.py` from `sp1-constraint-compiler --chip SyscallCore --format lean`. Contains every `SyscallCore` main-trace column in upstream index order, the complete per-row `assertZero` list, and the complete interaction list. The flat vector deliberately avoids inventing semantic field names in the extraction layer; audited adapters may name selected indices above this anchor. Reuses the no helper module(s). -/

set_option linter.all false  -- auto-generated: skip linters

namespace SP1Clean.Extracted
open SP1Clean

-- Generated Lean code for chip SyscallCoreChip

structure SyscallCoreCols (F : Type) where
  values : (Vector F 10)
deriving ProvableStruct

namespace SyscallCoreCols

@[irreducible] def asserts {F : Type} [Field F] [CoeHead F ℕ]
  (cols : (SyscallCoreCols F))
  (_preprocessed : (Vector F 0))
  (_publicValues : (Vector F 160))
  : List F :=
  let E0 : F := cols.values[9] - 1
  let E1 : F := cols.values[9] * E0
  let E2 : F := cols.values[9] * cols.values[9]
  let E3 : F := E2 * cols.values[9]
  let E4 : F := cols.values[9] * cols.values[9]
  let E5 : F := E4 * cols.values[9]
  let E6 : F := E3 - E5
  [
    E1,
    E6,
  ]

@[irreducible] def interactions {F : Type} [Field F] [CoeHead F ℕ]
  (cols : (SyscallCoreCols F))
  (_preprocessed : (Vector F 0))
  (_publicValues : (Vector F 160))
  : List (Interaction F) :=
  let E7 : F := cols.values[3] * 256
  let E8 : F := cols.values[2] + E7
  [
    ⟨.send, (.byte 3 0 cols.values[2] 0), cols.values[9]⟩,
    ⟨.send, (.byte 6 cols.values[3] 16 0), cols.values[9]⟩,
    ⟨.receive, (.raw .syscall [cols.values[0], cols.values[1], cols.values[2], cols.values[3], cols.values[4], cols.values[5], cols.values[6], cols.values[7], cols.values[8]]), cols.values[9]⟩,
    ⟨.send, (.raw .global [cols.values[0], cols.values[1], E8, cols.values[4], cols.values[5], cols.values[6], cols.values[7], cols.values[8], 1, 0, 8]), cols.values[9]⟩,
  ]

end SyscallCoreCols

end SP1Clean.Extracted
