# Axiom & trust ledger

> **Machine-generated snapshot — regenerate with `scripts/run_audit.sh` before relying on it.**
> Snapshot of 2026-06-10 (sp1-lean `e076768` + audit changes + the W1a capstone-premise split); raw
> probe output: [`axiom-census.txt`](axiom-census.txt) (314 probes, generated by
> `scripts/gen_axiom_probe.py`, gated by `scripts/run_audit.sh` — the gate **passes**: `sorryAx`
> reachable only from the known-debt set).

## How to read the axiom sets

| Axiom | Meaning | Status |
|---|---|---|
| `propext`, `Classical.choice`, `Quot.sound` | the three standard Lean/mathlib axioms | **baseline** — "axiom-clean" = exactly these |
| `Lean.ofReduceBool`, `Lean.trustCompiler` | `bv_decide`/`native_decide` trusted core | **accepted, disclosed** (Bitwise/Mul decision procedures, the witness battery, `plat_clint_base`). 18 `native_decide` occurrences in 15 files — the previous ledger's "no `native_decide` in `SP1Clean/`" line was **wrong**. |
| `sys_enable_experimental_extensions`, `load_reservation`, `match_reservation`, `plat_term_write` | LeanRV64D platform hooks on the bridged execute paths | **Sail-model trust base** |
| the ~70 softfloat/randomness hooks (`riscv_f16/32/64*`, `get_16_random_bits`, `cancel/valid_reservation`, …) | the **full** LeanRV64D platform surface | enters **only** the two `TargetVm` theorems, through the `try_step` *statement* (not a proof gap) |
| `sorryAx` | an incomplete proof | **project debt** — exactly the carriers below |

## The census, by bucket (314 probes)

| Bucket | # | Members (summary) |
|---|---|---|
| baseline (clean-3) | 236 | chip soundness + most completeness, faithfulness anchors, ALU/UType bridges + `kind`s, the GatedVm core: `GatedVm.exists_trail`, `balanced_state_bus`→`state_trail_of_balance`, `chipRows_step_sound`, `gatedExecution_of_specs_and_balance`, `isConsistentBalanced_of_intCast_zero`, and the W1a balance translation `sp1_state_balance_of_balancedInteractions` |
| + `oRB`/`tC` | 14 | **`MulChip.soundness`** (settling the previously-open question: `bv_decide` bucket, not clean-3) + the 13 `WitnessTests.*_conforms` anchors |
| + Sail (loads) | 21 | `correct_load_*_native`, `l*_chip_reaches_sail`, `Load*Chip.kind`: + `sys_enable…`, `load_reservation`, `plat_term_write` |
| + Sail (stores) | 12 | store bridges/`kind`s: `match_reservation` instead of `load_reservation` |
| + Sail (control flow) | 9 | Jal/Jalr/Branch bridges + `kind`s: + `sys_enable…` only |
| + Sail (aggregates) | 6 | `allChipKinds(_length)`, `gatedExecution_allChips`, `coverage_kinds_eq_registry`, `coverage_length`, `routeOf_reaches_sail` — the union footprint of all 25 `kind`s |
| + `oRB`/`tC` + `sorryAx` | 7 | the capstone chain: `sp1_witness_decode` (the decode-seam premise) and its consumer `sp1_gatedExecution_prereqs` (now a proven assembly, sorried only via the seam), and — via the **embedded completeness fields** of the chip `circuit`s, see note — `sp1Tables`, `sp1Tables_length`, `sp1GatedVm`, `sp1FormalEnsemble`, `sp1_machine_soundness` |
| baseline + `sorryAx` | 1 | `DivRemChip.completeness` |
| `propext` only / none | 4 | the `by decide` coverage guards (`covered_iff_routed`, `reachable_subset_wired`, `wired_subset_reachable`); `sp1Assumptions` |
| baseline + full Sail surface | 1 | **`Target.sp1_target_execution`** — the target machine-level theorem: **no `sorryAx`, no `bv_decide`**; the ~76 platform axioms enter through the `try_step` statement |
| ditto + `oRB`/`tC` + `sorryAx` | 1 | `Target.sp1_target_soundness` — routes through `sp1_machine_soundness`, inheriting its chain |

**The embedded-completeness note (TB-10).** `sp1Tables` wires each chip as `⟨chip.circuit⟩`, and a
`GeneralFormalCircuit` *contains* its `completeness` proof as a field — so the remaining `DivRem`
completeness `sorry` surfaces on the whole capstone chain under `#print axioms` even though the
ensemble soundness proof never consumes it. (`ShiftLeft`/`ShiftRight` completeness were closed
2026-06-12 — honest hint-driven `Populate` witness closures.) Closing DivRem plus the decode seam
(W1b/W1c) is what makes `sp1_machine_soundness`'s census clean. Conversely `sp1_machine_soundness` does **not** carry the
Sail-model axioms today — its proof threads the abstract premise, not the concrete chip dispatch;
closing W1 wires the 25 `kind`s (and their Sail footprint, as in `gatedExecution_allChips`) through.

## Complete `sorryAx` carrier set (9, all gated by the harness)

Direct holes: `DivRemChip.completeness`, `Soundness.sp1_witness_decode` (the decode seam).
Structural inheritors:
`sp1_gatedExecution_prereqs` (proven assembly over the seam), `sp1Tables(_length)`, `sp1GatedVm`,
`sp1FormalEnsemble`, `sp1_machine_soundness`, `Target.sp1_target_soundness`. **No soundness theorem,
no bridge, no faithfulness anchor, and not `Target.sp1_target_execution` carries `sorryAx`.** No
`axiom` declarations exist in `SP1Clean/`.

## Reproduce

```bash
lake build SP1Clean        # green; the only warnings are the two known sorries
scripts/run_audit.sh       # pins + inventory gates + the 314-probe census + the sorryAx gate
```

The probe (`scripts/axiom_probe.lean`) is regenerated by the harness from the live tree
(`scripts/gen_axiom_probe.py` — a namespace-tracking declaration scanner; wrong names fail
elaboration, so a green run certifies the census covers real declarations). Do **not** rely on
`grep sorry` alone or on `lean_verify` against a stale LSP — only `#print axioms` over current oleans
catches transitive carriers (e.g. the embedded-completeness inheritance above).
