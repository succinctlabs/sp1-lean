# Memory Index

- [Field-genericization effort current state](project_field_generic_effort.md) — additive `_poly` strategy; foundation+ops done; Track C1: **21 chips** with at least one closed `correct_*_poly`. **DivRem scaffolding 2026-05-08**: API surface complete (`div_rem_poly` stub + 8 `correct_<v>_poly` stubs in `DivRemChip.lean`); proof bodies + 8 `spec.<v>_poly` wrappers + `divw_remw_poly` h_abs/h_sign deferred. ShiftLeft/ShiftRight not started.
- [Mul iff_poly port has bound-source mismatches](feedback_mul_iff_poly_complexity.md) — literal port of MulOperation.allHold_constraints_iff_is_real to ZMod p doesn't close; bounds come from byte sends (field-level `<`) + sub-constraint lists (`.val <`) + carry/product bounds with no clean polymorphic source.
- [ShiftLeft generic iff_poly is intractable](feedback_shiftleft_iff_poly_blowup.md) — 195-let constraints body OOMs simp/tauto/simp_all when proving allHold_constraints_iff_poly; expect same for DivRem/Mul/Bitwise/Branch.
- [Shift chips embed KB-specific 64⁻¹ literal](feedback_shift_kb_specific_literal.md) — ShiftLeft/ShiftRight constraints use `2097414145` (`= 64⁻¹ mod KB`); not polymorphic. Mul/DivRem use only universal literals. Grep `\* [0-9]{8,}` to detect.
- [Polymorphic _poly proof patterns](feedback_poly_proof_patterns.md) — 5 tactical workarounds (simp_all strips Facts; cstrs shadowing; struct projection simp; field-< vs .val<; ZMod.val k reduction for ByteOpcode.ofNat).
- [Bitwise chip _poly patterns (multi-variant single iff)](feedback_bitwise_chip_patterns.md) — 3 patterns for chips with shared iff: sum_eq_one_* + single_op_poly helpers; push_cast;ring for opcode arg reduction; simp op-constraints into h_bop to unfold .1 projection.
- [AddrAddOp _poly resolved 2026-05-02](feedback_addradd_poly_blocker.md) — `spec_of_constraints_poly` landed (commit `909799e`) via AddOperation.spec_poly's recipe + skipKernelTC. Skipped cols_is_a_sum_b_poly intermediate (% 2^64 type triggered kernel deep recursion). 8 Store/Load chip migrations unblocked.
- [Chip-level _poly migration tactics](feedback_chip_migration_tactics.md) — 7 chip-side frictions (motive error on `← is_subw`; simp_all collapses `Fact (2^17<p)` to `Fact True`; missing val_X_zmod_p for opcode literals; `simp [← BitVec.toNat_inj]` recursion; sign_extend simp set; upfront h_is_real simp blocks iff_is_real_poly; aesop recursion on isInitialized side-goals → `clear *- hs; aesop`).
- [Chip _poly destructure blocked → resolved via C1](feedback_chip_poly_destructure_blocker.md) — Fin KB vs ZMod KB defeq blocks Track B `(p := KB)` ascription; resolved by chip-level parametric emission in update_constraints.py (PARAMETRIC_CHIPS dict, landed for SubChip 2026-05-01).
- [Grind ring init fails on free Fin KB vars](feedback_grind_ring_finkb.md) — `intros; subst_vars; grind` sidesteps the `Lean.Grind.Semiring.natCast` mismatch.
- [Fin KB not_eq_inv contradiction pattern](feedback_fin_kb_not_eq_inv.md) — `rw [eqc]; simp only [sub_self, mul_zero]; exact absurd _ (by decide)` closes `¬x=y` from `not_eq_inv * (x - y) = 1`.
- [Mechanical Main[idx] shift + stop pattern](feedback_mechanical_shift_pattern.md) — Python script to shift `Main[k]→Main[k-1]` above a cutoff + inject `stop`, used to update chips after column removal.
- [is_trusted removal baseline commit](reference_is_trusted_commit.md) — commit e686807 is the ground truth for old indices and what shifted.
- [nlinarith doesn't scale past ~4 limbs](feedback_nlinarith_scaling.md) — for core_mul_poly-style proofs, replace `nlinarith` bound checks with `Nat.mul_le_mul (by omega) (by omega)` + `omega`; prove polynomial identity in factored form to avoid heavyweight final `ring`.
- [DivRem core _poly port blockers](feedback_divrem_core_port_blockers.md) — divu_remu_poly closed 2026-05-07; divuw_remuw_poly + divw_remw_poly h_prod closed 2026-05-08 (uncommitted, verified under `lake build`); h_abs and h_sign still sorry. Use `lake build` (passes tstack=400000); `lake env lean` masks errors via SO short-circuit.
- [Extra shells/lagging builds slow compile](feedback_extra_shells_slow_compile.md) — pkill stale lake/lean processes before each new build on this 16GB/8-core machine; LSP + builds together cause memory pressure.
- [divu_remu_poly Blocker 2 — `.cast` is Nat→ℤ not ZMod.cast](feedback_divrem_blocker2_int_cast.md) — Closer is `push_cast [ZMod.cast_eq_val]; rfl`; the `.cast` in the goal is the Int coercion inserted to type `BitVec.ofNat 64 (q : ℤ)`.
