# maxHeartbeats override baseline — the no-increase invariant (see scripts/check_heartbeats.sh).
# Format: <lib> <count>. `set_option maxHeartbeats` sites per library.
# The stretch goal is NO increases: fold the blowup (docs/agents/proof-patterns.md §"maxHeartbeats: the
# fold recipe") instead of raising a ceiling. If an addition is genuinely term-intrinsic (DivRem/Mul/Shift/
# Sail/Word or a generated AIR expression whose single dependency closure cannot be chunked — the KEEP-set),
# bump the number below in the same PR with a one-line justification in the commit.
# Ratcheting DOWN is always welcome — lower these when you remove overrides.
# +288: exact whole-chip list faithfulness for all 25 instruction AIRs (+238 Faithful sites),
# completed 4.31 chip proofs (+30 Proofs), and the timed-grounding rollout (+20 Soundness/Native);
# these are folded generated-expression closures, not higher ceilings on existing declarations.
# +104 (2026-07, oracle-migration completion): the 25 generated ChipOracle modules carry the
# uniform 8M elaboration boundary per asserts/interactions definition (173 sites vs the 74 in the
# retired legacy Extracted files — the oracle form embeds chip-private op copies, hence more
# opaque-definition headers), plus a net +2 hand-written in the Faithful DivRem/Mul/Addw KEEP-set
# from the anchor fold-ins. Generated elaboration boundaries, not raised ceilings.
# -13 (2026-07-27, proof-cleanup W1/g3): ratchet DOWN, no ceiling raised. ShiftRightChip/Dispatch.lean
# dropped all 12 of its 1000000 ceilings (the 16-way `first | exact …` ladder became `rcases … with
# rfl | rfl` + a hoisted `key` + ordered bullets, so ~8 failing alternatives per goal — each a full
# rw/push_cast/ring1 — are no longer elaborated and discarded; floor re-measured at (25k, 35k]), and
# Model/SailMemory.lean dropped its unmeasured 10000000 file ceiling (the Sail mem_read/translateAddr
# peels measure <=20k, so the family was carrying a default it never needed). Two sites were
# re-measured and KEPT, hence not counted here: ShiftRightChip/Core.lean relocated its 2000000
# file-scope ceiling to a scoped 1600000 on the single binding lemma `srlw_within_byte_shift` (1->1
# site), and Model/SailDecode.lean ratcheted 8000000 -> 400000 (floor (40k, 50k]).
# -6 (2026-07-27, proof-cleanup W1/g4): ratchet DOWN, no ceiling raised. Removals, all measured:
# Math/Word.lean dropped the 2000000 on `toBitVec64_signExtend_word` (floor (2000, 5000] — `Math/`
# now carries zero ceilings), Addw/SubwOperation/RawSpec.lean dropped all four 16000000 ceilings
# (floors <=40k — the same carry-over the Add/Sub/AddrAdd RawSpec family showed), and
# AddrAddOperation/Populate.lean dropped its 4000000 (floor (6000, 10000]). One site was relocated,
# hence not counted: Model/SailWrap.lean retired its file-scoped 10000000 — which covered all 57
# declarations — for a scoped 400000 on the single binding lemma `rX_bits_eq_get_reg?` (floor
# (50k, 100k]; 1->1 site). The two write_reg commutation lemmas that actually drove the old file
# ceiling were folded instead: a permutative `@[simp] Std.ExtDHashMap.insert_insert_comm` was being
# ordered by `Lean.Meta.acLt` across a `▸` cast whose proof is a 31-arm match; `unfold` + two
# `generalize`s take them from >400k to under the plain default.
# -4 (2026-07-28, proof-cleanup W2/r1): ratchet DOWN, no ceiling raised. All four removals were
# measured by ladder and every one sat 200-400x over its floor:
# U16toU8OperationSafe.lean dropped both 1000000 ceilings, SubOperation/Populate.lean and
# BitwiseOperation/Populate.lean each dropped a 2000000 on `spec_populate` (floors in (5000, 10000]
# for both — the structurally identical AddOperation.spec_populate never carried one). The measured
# ladders are recorded in the doc-comments at each former site.
# -14 (2026-07-28, proof-cleanup W2/r2): ratchet DOWN, no ceiling raised. The whole
# `Proofs/Operations/*/Formal.lean` soundness/completeness family shed every ceiling it carried —
# eight 1000000 and six 2000000 sites, uniformly two per file, across AddOperation, SubOperation,
# AddrAddOperation, BitwiseOperation, U16MSBOperation, U16CompareOperation and IsZeroOperation.
# The duplicated-fact check ran first and did find real duplication, but that was NOT the ceilings'
# cause: every floor was already 30-100x under its ceiling BEFORE any golf, so this family was
# carrying a plain inherited default rather than masking a whnf blowup (unlike the shift cores in
# W1/g3). Not a single site was within 30x of its measured floor. The ladders are recorded in the
# doc-comments at each former site.
# -15 (2026-07-28, proof-cleanup W2/r3): ratchet DOWN, no ceiling raised. 17 directives deleted, 2
# re-added strictly lower, net -15. By batch: the type-reader family shed both 4000000 sites in
# ALUTypeReaderImmutable.lean (b1); AddressOperation.lean dropped three 4000000 and
# BitwiseU16Operation.lean all four 4000000 (b2); Faithful/CPUState.lean dropped both 1000000 (b4).
# Two results from this round are worth recording because they are the campaign's first counter-
# examples to the "every ceiling is an inherited default" pattern:
#   * LtOperationUnsigned/RawSpec.lean removed three 1000000 sites but KEPT the fourth --
#     `at_most_one` measured a floor in (400k, 500k] against its declared 1000000, the first
#     correctly-sized ceiling found. Its three companions were carry-overs of that justified number
#     sideways within the same file, and those are the three that went.
#   * MulOperation/RawSpec.lean `full_product` is the first TERM-INTRINSIC ceiling: floor 1.0-1.2M,
#     the highest of the ~110 sites measured so far, driven by a 16-column `omega` telescope over
#     2^128 literals plus a 256-monomial `ring`. It is not foldable, so it was kept and merely
#     tightened 16000000 -> 6000000 (~5x headroom). The same file removed one 8000000 outright and
#     tightened a third site 1600000 -> 500000.
# Faithful/CPUState.lean's measured floors (800-2500 against a declared 1000000) are the lowest
# recorded anywhere in the campaign and predict that the large majority of the ~300 remaining
# Faithful/ ceilings are removable. All ladders are recorded in the doc-comments at each site.
# -28 (2026-07-28, proof-cleanup W2/r4): ratchet DOWN, no ceiling raised. 29 directives deleted, 1
# re-added strictly lower, net -28 — the largest single-round drop of the campaign. By batch:
# Faithful/ChipOracle.lean shed ALL ELEVEN of its 1000000 sites (b1), with measured floors from
# <=500 up to 30000 against a uniform declared 1M; the cause was proved from inside the file, since
# three unceilinged theorems doing the same kind of work sit interleaved among the eleven — i.e. the
# ceilings were copied down the file, not measured. The Proofs/Operations/*/Formal.lean family
# finished the sweep W2/r2 began: LtOperationUnsigned (4), SubwOperation (3), AddwOperation (3),
# IsZeroWordOperation (2) (b2). Faithful/ALUTypeReader.lean dropped all four and
# Native/Operations/LtOperationSigned/RawSpec.lean its remaining one (b4).
# Two results are worth recording:
#   * MulOperation/Defs.lean is the campaign's first LCNF-COMPILER-BOUND ceiling: the budget is
#     spent in code generation over 16 schoolbook product expressions, not in elaboration (at a
#     40000 budget `main` elaborates fine and only codegen times out). Floor (40000, 60000]; kept
#     and tightened 8000000 -> 400000 (~6.7x headroom) rather than removed, because the plain
#     200000 default leaves under 3.5x and codegen cost is more load-sensitive than elaboration.
#     `noncomputable def` was rejected: SP1CleanTest/TraceGenTests derives traces from `main`'s
#     witness closures under `native_decide`, so it would break `lake test`.
#   * b2 found that `ring` CANNOT LEAD a `first` ladder — it never fails, so it shadows every
#     alternative behind it (and its ring_nf fallback is what leaks the `Try this:` info note).
#     `ring1` is the correct leading alternative. This was also the round's first cause fix that
#     moved a ceiling from keep-and-lower to removable (`sel_populate`).
# All ladders are recorded in the doc-comments at each former site.
# -51 (2026-07-28, proof-cleanup W3/r1): ratchet DOWN, no ceiling raised. This is 50 REAL removals
# plus the retirement of the long-standing PHANTOM described in the note below — read the two
# together, because 738 -> 687 does NOT correspond to 51 live sites disappearing.
# By batch: Proofs/Sail/Advance.lean shed ALL 35 of its real ceilings (b1) and now carries ZERO,
# the campaign's largest single-file result; six Faithful/ files shed all 14 of theirs (b2) —
# RTypeReader (5), ITypeReaderImmutable (4), AddOperation (2), IsZeroOperation/U16MSBOperation/
# U16CompareOperation (1 each), confirming W2/r3's prediction that the bulk of Faithful/ is
# removable; MulOperation/Formal.lean removed one 4000000 and tightened its two 40000000 sites to
# 1000000 (b4). Soundness/TimedGrounding.lean (b3) carried no ceilings and removed none.
# The round's headline result is a CAUSE FIX, not a clearing pass:
#   * Advance.lean's file-scoped 4000000 — which covered all ~200 declarations of a 3950-line file —
#     was owned in its entirety by ONE tactic call: `grind [reg_idx_must_64]` in `get_reg?_writeBack`.
#     `grind` had to whnf through `reg_idx_must_64`'s 31-arm `match` on `Register`: the same
#     dependent-cast hazard Model/SailWrap.lean documents at its `acLt` fix, reached through `grind`
#     rather than a permutative `simp`. Replacing it with explicit cast cancellation
#     (`eqRec_eq_cast` -> `cast_eq_iff_heq` -> `cast_heq`) cleared the file entirely: it now
#     elaborates in 23s at the plain default, and all 34 remaining scoped sites fell out with it.
#     The theorem remains [propext, Classical.choice, Quot.sound].
# PHANTOM RETIRED: the note below has applied to every baseline this campaign recorded. Advance.lean
# spelled the literal directive text inside a doc-comment, and check_heartbeats.sh greps raw text, so
# that prose was counted as a live ceiling throughout. b1 rewrote the prose, so exactly one unit of
# the -51 is this bookkeeping correction with NO site changing. The recorded count is now honest:
# as of this round there is no known phantom, and 687 should equal the number of real directives.
# -16 (2026-07-28, proof-cleanup W3/r2): ratchet DOWN, no ceiling raised. 16 directives deleted, 0
# re-added — and the count reconciles EXACTLY (687 - 16 = 671), the first round since the phantom
# retirement to confirm the recorded number is honest. By batch: DivRemChip/Populate/Euclid.lean shed
# all TEN of its ceilings (b1) — five 400000, two 800000, one 1000000, one 1600000 and one further
# 800000; three chip `elaborated` instances dropped one each, MulChip/BitwiseChip/LtChip (b2) and
# AluX0Chip (b3); DivRemOperation/Compare.lean dropped both of its (b4).
# The round's headline result is b1's CAUSE FIX, the campaign's best efficiency at 0.4 passes/site:
#   * Euclid.lean's ten ceilings were all downstream of ONE unspelled fact — the base-2^16 carry
#     split `s = s % 2^16 + (s / 2^16) * 65536`. Each of the eight `chain_residue_field_i` lemmas
#     re-derived it inline as a 15-line tactic proof, and the aggregate/class-core lemmas above them
#     paid for eight copies of that derivation. Hoisting it to one `private lemma
#     residue_carry_split` turned each of the eight into a 6-line term proof (-70 lines) and every
#     ceiling in the file fell out with it. Four further private helpers (`remExtNat_eq`,
#     `remExtNat_lt`, `bExtNat_lt`, `setWidth32_toNat`) collapsed the repeated bound/aggregate
#     plumbing at the two signed class cores. `euclid_limbs_sW` and `chain_residue_field_7` remain
#     [propext, Classical.choice, Quot.sound].
#   * b4's two sites (DivRemOperation/Compare.lean) are the round's second cause fix: a single
#     `simp_all` under `circuit_proof_start` owned the whole file's budget. Narrowed to
#     `rcases` + `rw` + `simp` (with a hoisted `eval_getElem` private helper), the measured floor
#     dropped from (40000, 100000] to under 40000 and both sites became removable.
#     `DivRemCompare.soundness` remains [propext, Classical.choice, Quot.sound].
# Also landed this round with no ceiling effect: 22 `Native/Chips/<Chip>/Defs.lean` files (b2+b3)
# deleted the `ElaboratedCircuit` `localLength_eq`/`output_eq` Prop fields wherever Clean's default
# tactic closes them. That is a PERFORMANCE decision, not a style one — the default `intros; rfl`
# whnf's the whole chip `main` — so all 19 affected files were A/B-timed before the omission was
# kept. b2 found the omission REGRESSES +63-132% on the three chips whose `main` composes
# `U16MSBOperation`, and fails outright on two; those kept their hand-written fields. Every retained
# omission measured neutral-or-better (-3% to 0%). The family is split 15 explicit-`where` /
# 6 `elaborate_circuit` / 1 private-derivation forward.
# -3 (2026-07-28, proof-cleanup W3/r3a): ratchet DOWN, no ceiling raised. 5 directives deleted, 2
# re-added strictly lower, net -3; the count reconciles EXACTLY (671 - 3 = 668), the second
# consecutive round to confirm no phantom. By batch: DivRemOperation/Core.lean removed a 64000000
# and a 16000000 outright and tightened its two remaining 16000000 sites to 500000 and 400000 (b1);
# DivRemOperation/Compare.lean dropped its last 4000000, on the `elaborated` instance, and now
# carries zero (b3). FormalModel/Contracts/Chips.lean (b2) carried none.
# This round's interest is NOT the ceiling count — it is the campaign's LARGEST SINGLE-FILE LINE
# RESULT and the first KEEP-set ceiling pair confirmed correctly-sized at both ends:
#   * DivRemOperation/Core.lean went 1172 -> 706 lines (-466, -40%). The file hand-wrote the same
#     two-line `rw [← h, Vector.getElem_map]` eval-transport bridge 176 TIMES across three proofs.
#     Extracting it to one `private lemma eval_getElem` halved that to 176 one-liners; the second
#     and larger half was PARTIALLY APPLYING it — `have gvA := eval_getElem vA` yields a quantified
#     rewrite rule covering a whole index family, so 176 `have`s collapsed to 16 and the trailing
#     `simp only` shed 80 explicit arguments. 176 -> 25 lines. This generalises: wherever a proof
#     spells one bridge per (vector, index) pair, one partially-applied helper per vector suffices.
#     The same file's `selection_of_flags` dropped 56 lines by replacing eight blocks of seven
#     `have z_i : ... = 0` with `cases other <;> dsimp only [Case.flag] <;> first | ... | ...`.
#   * The two KEPT ceilings are the campaign's first pair measured genuine BEFORE AND AFTER a major
#     golf: `soundness` and `completeness` both fail at 40000 either way, so the golf did not create
#     the need and does not remove it. They were merely ~200x oversized at 16000000 and are now at
#     500000/400000 (~12x headroom over the measured floor).
#     Timing A/B, solo, same machine: 21.58s -> 15.79s CPU (10.56s -> 8.88s wall), i.e. the -40%
#     rewrite also bought -27% CPU. A large line reduction on a Tier-S file did not cost elaboration.
#   * b2 DECLINED a -4 on `rv64_subw_eq`: `bv_decide` closes it, but that would add
#     `Lean.ofReduceBool`/`Lean.trustCompiler` to an audit-surface lemma that is currently clean.
#     It stands unchanged at [propext, Quot.sound]. The three substitutions b2 DID make replace
#     hand-rolled `eq_of_toNat_eq` + `omega`/`Nat.mod_mod_of_dvd` chains with the library lemmas
#     `BitVec.setWidth_add` / `BitVec.setWidth_mul` (-8 lines on the audit surface).
#     GATE CORRECTION: b2 reported these as an axiom improvement ("addw/mulw dropped the
#     `Classical.choice` that `omega` had pulled in"). Measured before-and-after, that is NOT what
#     happened, and the note is recorded here so the claim is not carried forward:
#       - `rv64_addw_eq`  [propext, Quot.sound] -> [propext, Quot.sound]   (unchanged; it never
#         carried `Classical.choice` — `omega` had not leaked one)
#       - `rv64_mulw_eq`  [propext] -> [propext, Quot.sound]               (a strict ADDITION, not
#         a drop; negligible, since `Quot.sound` is inside the permitted set, but it is the
#         opposite direction from the one claimed)
#       - `rv64_mulh_eq`  unchanged in substance: [propext, Classical.choice, Quot.sound] plus the
#         same THREE pre-existing `bv_decide` axioms. Only the auto-generated index of the
#         `high64_mul` one moved (`ax_1_5` -> `ax_1_6`) because that proof term changed; no
#         `bv_decide` axiom was gained or lost.
#     Net: no lemma left the permitted axiom set in either direction, so the change is admissible —
#     but this round produced NO axiom improvement, and a reported one should be measured, not
#     inferred from which tactics were removed.
# -14 (2026-07-28, proof-cleanup W3/r3b+r3c): ratchet DOWN, no ceiling raised. 15 directives deleted,
# 1 re-added strictly lower, net -14; the count reconciles EXACTLY (668 - 14 = 654), the THIRD
# consecutive round to confirm the recorded number is honest and that no phantom has re-appeared.
# By batch (r3b): DivRemChip/Extract.lean dropped both of its (b2); Faithful/IsEqualWordOperation.lean
# dropped both 1000000 sites and DivRemOperation/Core.lean its last 4000000 — that file now carries
# ZERO, having entered W3/r3a with four (b3). By batch (r3c): LtOperationSigned/Formal.lean shed all
# four and IsEqualWordOperation/Formal.lean both (b1); Faithful/IsZeroWordOperation.lean both (b2).
# Two results are worth recording:
#   * r3c/b1 CLOSES the `Proofs/Operations/*/Formal.lean` family at 32/32 ceilings removed across
#     thirteen files (14 -> 26 -> 32 over W2/r2, W2/r4 and this round). Verified at the gate: all
#     thirteen now grep to zero. The fourteenth file, MulOperation/Formal.lean, is NOT part of that
#     32 — it is the deliberate KEEP-set member whose two sites W3/r1/b4 measured (floors 100k and
#     150k) and tightened 40000000 -> 1000000. So the family is closed, not merely swept: every
#     ceiling in it has now been either removed against a measured floor or kept against one.
#   * r3c/b2 produced the campaign's FIRST `Faithful/` KEEP, in a directory where the previous three
#     sweeps (W2/r3, W2/r4, W3/r1) had removed every ceiling they measured:
#     `ltUnsigned_constraints_faithful` measured a floor in (50000, 60000], driven by `itauto` over a
#     13-gate propositional goal. That search is exponential in the gate count and the goal is
#     genuinely propositional, so the §2.8 levers (fold, hoist, narrow the simp set) have nothing to
#     bite on. Kept and tightened 2000000 -> 400000 (~6.7x headroom). Its file-mate
#     `ltUnsigned_assertions_exact` was removable and went. The batch reports the running Faithful/
#     tally as 38 measured / 37 removed / 1 kept; the gate verified the KEEP itself, not that tally.
# 269 ceilings remain in Faithful/, all in the 25 whole-chip anchor files, which W2/r3's prediction
# still expects to be largely removable.
SP1Clean 654
SP1CleanTest 16
