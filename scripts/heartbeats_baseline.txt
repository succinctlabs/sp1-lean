# maxHeartbeats override baseline — the no-increase invariant (see scripts/check_heartbeats.sh).
# Format: <lib> <count>. `set_option maxHeartbeats` sites per library.
# The stretch goal is NO increases: fold the blowup (docs/agents/proof-patterns.md §"maxHeartbeats: the
# fold recipe") instead of raising a ceiling. If an addition is genuinely term-intrinsic (DivRem/Mul/Shift/
# Sail/Word or a generated AIR expression whose single dependency closure cannot be chunked — the KEEP-set),
# bump the number below in the same PR with a one-line justification in the commit.
# Ratcheting DOWN is always welcome — lower these when you remove overrides.
# +288: exact whole-chip list faithfulness for all 25 instruction AIRs (+238 Faithful sites),
# completed 4.31 chip proofs (+30 Proofs), and the timed-grounding rollout (+20 Soundness/Native);
# these are folded generated-expression closures, not higher ceilings on existing declarations.
# +104 (2026-07, oracle-migration completion): the 25 generated ChipOracle modules carry the
# uniform 8M elaboration boundary per asserts/interactions definition (173 sites vs the 74 in the
# retired legacy Extracted files — the oracle form embeds chip-private op copies, hence more
# opaque-definition headers), plus a net +2 hand-written in the Faithful DivRem/Mul/Addw KEEP-set
# from the anchor fold-ins. Generated elaboration boundaries, not raised ceilings.
SP1Clean 818
SP1CleanTest 16
