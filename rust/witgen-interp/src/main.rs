//! Reference interpreter for the Lean-exported witness-generation IR.
//!
//! Scaffold (wave D1). The parser, KoalaBear field arithmetic, evaluator, and the
//! differential test against SP1-dumped trace fixtures land in wave D4; the wire format
//! it implements is pinned by `docs/witgen-wire-format.md`.

fn main() {
    eprintln!("witgen-interp: scaffold only — the interpreter lands in wave D4.");
    std::process::exit(2);
}
